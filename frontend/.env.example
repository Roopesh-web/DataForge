# Leave empty during local Vite development so requests use the Vite proxy
# (same-origin) and avoid browser CORS blocks against the API.
# Example direct URL (requires CORS to allow the Vite origin):
# VITE_API_URL=http://localhost:8000
VITE_API_URL=
